server:
  port: 7071
spring:
  application:
    name: config-server  # 应用名称
  cloud:
    config:
      server:
        git:
          uri: https://gitee.com/Chengzhen2925/config-server.git  #配置文件Git 地址
          default-label: master #配置文件分支
          search-paths: config-center  #配置文件所在根目录